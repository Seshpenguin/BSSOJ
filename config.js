//BSS OJ Config File.

var config = {};
config.DB = {};

// this password is used to authenticate all the server modules. Keep it safe!
// make sure it is long.
config.serverAuthPassword = "SomePassword123"; 
config.DB.hostname = "wow"

module.exports = config;